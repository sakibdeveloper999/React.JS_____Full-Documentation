
---

## 🧩 এক বাক্যে সারসংক্ষেপ

**Props Drilling** হলো এমন একটি অবস্থা যেখানে তুমি কোনো ডেটা বা ফাংশন উপরের কম্পোনেন্ট (parent) থেকে নিচের অনেক স্তরের (deep nested) কম্পোনেন্টে পাঠাও — মাঝখানে থাকা অনেকগুলো কম্পোনেন্ট আসলে সেই ডেটা ব্যবহারই করে না, শুধু পাস করে দেয়।
ফলে কোড জটিল, রক্ষণাবেক্ষণ কঠিন এবং অপ্রয়োজনীয় হয়ে যায়।

---

## 🔹 Step by Step: Props Drilling কীভাবে কাজ করে

1️⃣ তুমি কোনো ডেটা বা state parent কম্পোনেন্টে রাখলে।
2️⃣ সেই ডেটা deep child কম্পোনেন্টে দরকার হলো।
3️⃣ তুমি `props` হিসেবে parent → child → grandchild → ... করে পাঠালে।
4️⃣ মাঝের কিছু কম্পোনেন্ট সেই props ব্যবহার না করে, শুধু পরেরটিকে পাঠায়।
👉 এটিই “props drilling” নামে পরিচিত।

---

## 🧠 Example (সমস্যা বোঝার জন্য)

```jsx
// App.jsx
import React, { useState } from "react";
import Parent from "./Parent";

export default function App() {
  const [user, setUser] = useState({name: "Sakib", loggedIn: true});
  return <Parent user={user} setUser={setUser} />;
}

// Parent.jsx
import React from "react";
import Middle from "./Middle";
export default function Parent({ user, setUser }) {
  return <Middle user={user} setUser={setUser} />;
}

// Middle.jsx
import React from "react";
import Child from "./Child";
export default function Middle({ user, setUser }) {
  // Middle কম্পোনেন্ট `user` ব্যবহারই করছে না, শুধু নিচে পাঠাচ্ছে
  return <Child user={user} setUser={setUser} />;
}

// Child.jsx
import React from "react";
export default function Child({ user, setUser }) {
  return (
    <div>
      <p>User: {user.name} ({user.loggedIn ? "online" : "offline"})</p>
      <button onClick={() => setUser({...user, loggedIn: !user.loggedIn})}>
        Toggle
      </button>
    </div>
  );
}
```

### ✴️ সমস্যা:

* `Parent` ও `Middle` কম্পোনেন্ট কোনো ডেটা ব্যবহার করছে না, শুধু পাস করছে।
* যদি নতুন কোনো প্রপস দরকার হয়, তাহলে সব কম্পোনেন্টে পরিবর্তন আনতে হয়।
* কোড বড় হলে এই পদ্ধতি খুব ঝামেলাপূর্ণ হয়ে যায়।

---

## ⚠️ Props Drilling এর সমস্যা

1. **Boilerplate code:** অনেক কম্পোনেন্টে অপ্রয়োজনীয় props পাস করতে হয়।
2. **Maintenance কঠিন:** একটি prop পরিবর্তন মানে অনেক ফাইলে পরিবর্তন।
3. **Readability কমে যায়:** আসল ব্যবহার কোথায় হচ্ছে বোঝা কঠিন।
4. **Reusability কমে যায়:** মধ্যবর্তী কম্পোনেন্ট পুনর্ব্যবহার করা কঠিন।
5. **Performance issue:** অনেক re-render হয় কারণ props চেইনে নেমে যায়।

---

## ✅ কখন Props Drilling ঠিক আছে

* কম্পোনেন্ট ট্রি ছোট হলে (১–২ লেভেল গভীর)।
* ডেটা শুধুমাত্র নির্দিষ্ট ছোট অংশে দরকার হলে।
* অতিরিক্ত Context API বা state management যোগ না করতে চাইলে।

---

## 🔄 বিকল্প পদ্ধতি (Alternatives)

### 🟢 1. **React Context API** (সবচেয়ে জনপ্রিয় সমাধান)

যখন ডেটা অনেক লেভেলের নিচে দরকার হয় বা অনেক কম্পোনেন্টে শেয়ার করতে হয়।

#### 🔧 Refactor Example (Context ব্যবহার করে)

```jsx
// UserContext.js
import React from "react";
export const UserContext = React.createContext(null);

// App.jsx
import React, { useState } from "react";
import { UserContext } from "./UserContext";
import Parent from "./Parent";

export default function App() {
  const [user, setUser] = useState({name: "Sakib", loggedIn: true});
  const value = { user, setUser };
  return (
    <UserContext.Provider value={value}>
      <Parent />
    </UserContext.Provider>
  );
}

// Child.jsx (deep component)
import React, { useContext } from "react";
import { UserContext } from "./UserContext";

export default function Child() {
  const { user, setUser } = useContext(UserContext);
  return (
    <div>
      <p>{user.name} — {user.loggedIn ? "online" : "offline"}</p>
      <button onClick={() => setUser(prev => ({...prev, loggedIn: !prev.loggedIn}))}>
        Toggle
      </button>
    </div>
  );
}
```

🔹 **নোট:**

* শুধুমাত্র সেই subtree ঘিরে `Provider` দাও যেখানে দরকার।
* `useMemo` ব্যবহার করে `value` মেমোইজ করলে পারফরম্যান্স ভালো হয়।
* Context ছোট শেয়ার করা state এর জন্য ভালো; বড় গ্লোবাল state এর জন্য নয়।

---

### 🟣 2. **Lifting State Up**

যদি দুইটি sibling কম্পোনেন্টে state দরকার হয়, তাহলে সেটি common parent এ নিয়ে যাও এবং props হিসেবে পাঠাও।

---

### 🔵 3. **Custom Hook + Composition**

একটি custom hook (যেমন `useAuth`) তৈরি করে তার ভেতরে Context বা Logic রাখো — সহজ ও পুনর্ব্যবহারযোগ্য।

---

### 🟠 4. **State Management Library**

বড় অ্যাপ্লিকেশনে যখন state অনেক জটিল হয়, তখন ব্যবহার করো:

* **Redux / Redux Toolkit**
* **Zustand / Jotai / Recoil**

---

### 🟢 5. **Render Props / Children as Function / Composition**

প্রপস না পাঠিয়ে ফাংশন হিসেবে child দেয়া যায়, যাতে সে সরাসরি ডেটা পায়।

---

## 💡 ভালো প্র্যাকটিস (Best Practices)

✅ ছোট scope হলে props drilling করো।
✅ বড় scope হলে Context ব্যবহার করো।
✅ Context কে ভাগ করে রাখো (AuthContext, ThemeContext)।
✅ `useMemo` দিয়ে provider এর value স্ট্যাবল রাখো:

```js
const value = useMemo(() => ({ user, setUser }), [user]);
```

✅ React.memo ব্যবহার করে অপ্রয়োজনীয় re-render রোধ করো।
✅ Context শুধু প্রয়োজনীয় subtree তেই wrap করো।
✅ Provider নামগুলো স্পষ্ট রাখো (যেমন AuthProvider)।
✅ React DevTools দিয়ে re-render trace করো।

---

## 🧰 Debugging Tips

* কোন কম্পোনেন্ট props ব্যবহার করছে তা খুঁজতে `search` করো।
* Console.log দিয়ে props flow ট্রেস করো।
* React DevTools দিয়ে props/context ইনস্পেক্ট করো।
* ইউনিট টেস্টে সরাসরি mock props দাও।

---

## 🧾 Quick Decision Checklist

| পরিস্থিতি                       | সমাধান                   |
| ------------------------------- | ------------------------ |
| Props ১–২ লেভেল নিচে দরকার      | সাধারণ props পাস         |
| অনেক কম্পোনেন্টে দরকার          | Context API              |
| জটিল অ্যাপ, অনেক state          | Redux / Zustand          |
| অনেক প্রপস ফরোয়ার্ড করতে হচ্ছে | Context এ রিফ্যাক্টর করো |

---

## 🪜 Refactor Plan (Props Drilling থেকে মুক্তি)

1️⃣ কোন props বারবার নিচে পাঠানো হচ্ছে তা খুঁজে বের করো।
2️⃣ Context Provider তৈরি করে ঐ subtree তে wrap করো।
3️⃣ Consumer এ `useContext` ব্যবহার করো।
4️⃣ React DevTools দিয়ে render behavior চেক করো।
5️⃣ বড় অ্যাপে প্রয়োজনে Redux বা Zustand নাও।

---

## 🎯 সারসংক্ষেপ

| বিষয়               | ব্যাখ্যা                                     |
| ------------------ | -------------------------------------------- |
| **Props Drilling** | ডেটা একাধিক স্তরে অপ্রয়োজনীয়ভাবে পাস করা   |
| **সমস্যা**         | জটিল কোড, maintain কঠিন                      |
| **সমাধান**         | Context API, state management, custom hook   |
| **Best Practice**  | ছোট scope → props, বড় scope → context/store |

---
