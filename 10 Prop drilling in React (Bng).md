এটি React.js-এ **Prop Drilling** নিয়ে পূর্ণ স্টেপ-বাই-স্টেপ ডকুমেন্টেশন, যেখানে রয়েছে এর কাজের পদ্ধতি, সমস্যা, বিকল্প সমাধান (যেমন Context API), এবং Best Practices।

---

# 🧩 এক বাক্যে সারসংক্ষেপ

**Prop drilling** হলো এমন একটি অবস্থা যেখানে তুমি কোনো ডেটা বা ফাংশন অনেক স্তরের কম্পোনেন্টে প্রপস হিসেবে পাঠাও — অথচ মাঝের কম্পোনেন্টগুলো ডেটাটি ব্যবহারই করে না।
ফলাফল: কোড জটিল, ভুল হওয়ার সম্ভাবনা বেশি, এবং প্রজেক্ট বড় হলে maintain করা কঠিন হয়ে যায়।

---

## 1️⃣ Prop Drilling কীভাবে কাজ করে (Step-by-Step)

1. তোমার কাছে কোনো state বা value আছে parent বা উপরের কোনো ancestor কম্পোনেন্টে।
2. সেই ডেটা দরকার নিচের অনেক গভীর child কম্পোনেন্টে।
3. তুমি parent → child → grandchild → ... করে props পাঠাও যতক্ষণ না সেটি consumer কম্পোনেন্টে পৌঁছে।
4. মাঝের কম্পোনেন্টগুলো props শুধু গ্রহণ করে আবার নিচে পাঠায় — তারা ডেটা ব্যবহার করে না।
   👉 এই forwarding প্রক্রিয়াটাকেই বলা হয় **Prop Drilling**।

---

## 2️⃣ ছোট উদাহরণ (যেখানে সমস্যা বোঝা যায়)

```jsx
// App.jsx
import React, { useState } from "react";
import Parent from "./Parent";

export default function App() {
  const [user, setUser] = useState({name: "Sakib", loggedIn: true});
  return <Parent user={user} setUser={setUser} />;
}

// Parent.jsx
import React from "react";
import Middle from "./Middle";
export default function Parent({ user, setUser }) {
  return <Middle user={user} setUser={setUser} />;
}

// Middle.jsx
import React from "react";
import Child from "./Child";
export default function Middle({ user, setUser }) {
  // Middle কম্পোনেন্ট `user` ব্যবহার করছে না, শুধু নিচে পাঠাচ্ছে
  return <Child user={user} setUser={setUser} />;
}

// Child.jsx (deep consumer)
import React from "react";
export default function Child({ user, setUser }) {
  return (
    <div>
      <p>User: {user.name} ({user.loggedIn ? "online" : "offline"})</p>
      <button onClick={() => setUser({...user, loggedIn: !user.loggedIn})}>
        Toggle
      </button>
    </div>
  );
}
```

### ⚠️ সমস্যাগুলো:

* `Parent` এবং `Middle` শুধু props ফরোয়ার্ড করছে, ব্যবহার করছে না।
* যদি `Child`-এ নতুন কোনো props দরকার হয়, তাহলে `Parent` এবং `Middle` দুটোতেই পরিবর্তন আনতে হবে।
* প্রজেক্ট বড় হলে এই প্যাটার্ন খুব ঝামেলাপূর্ণ হয়ে যায়।

---

## 3️⃣ কেন Prop Drilling সমস্যা তৈরি করে?

* **অপ্রয়োজনীয় কোড (boilerplate):** অনেক কম্পোনেন্টে এমন props পাস করতে হয় যেগুলো তারা ব্যবহারই করে না।
* **Fragile refactor:** একটি prop পরিবর্তন করলে অনেক ফাইল পরিবর্তন করতে হয়।
* **কম রিডেবিলিটি:** কোন কম্পোনেন্ট আসলে ডেটা ব্যবহার করছে বোঝা কঠিন হয়।
* **Reusability কমে যায়:** মধ্যবর্তী কম্পোনেন্ট অন্য জায়গায় পুনর্ব্যবহার কঠিন।
* **পারফরম্যান্স সমস্যা:** props পরিবর্তন হলে সব intermediate কম্পোনেন্ট আবার render হয়।

---

## 4️⃣ কখন Prop Drilling ঠিক আছে

✅ যখন component tree খুব ছোট (১–২ লেভেল নিচে)।
✅ যখন ডেটা শুধুমাত্র একটি ছোট subtree তে দরকার।
✅ যখন অতিরিক্ত Context বা state management যোগ করতে চাও না।

---

## 5️⃣ বিকল্প সমাধান (Alternatives)

### 🟢 A — React Context API (সবচেয়ে জনপ্রিয় সমাধান)

যখন একই ডেটা অনেক গভীর বা অনেক কম্পোনেন্টে দরকার হয়, কিন্তু পুরো অ্যাপ জুড়ে নয়।

**উদাহরণ (Context ব্যবহার করে Refactor):**

```jsx
// UserContext.js
import React from "react";
export const UserContext = React.createContext(null);

// App.jsx
import React, { useState } from "react";
import { UserContext } from "./UserContext";
import Parent from "./Parent";

export default function App() {
  const [user, setUser] = useState({name: "Sakib", loggedIn: true});
  const value = { user, setUser };
  return (
    <UserContext.Provider value={value}>
      <Parent />
    </UserContext.Provider>
  );
}

// Child.jsx
import React, { useContext } from "react";
import { UserContext } from "./UserContext";

export default function Child() {
  const { user, setUser } = useContext(UserContext);
  return (
    <div>
      <p>{user.name} — {user.loggedIn ? "online" : "offline"}</p>
      <button onClick={() => setUser(prev => ({...prev, loggedIn: !prev.loggedIn}))}>
        Toggle
      </button>
    </div>
  );
}
```

🔹 **নোট:**

* শুধু সেই subtree তে `Provider` ব্যবহার করো যেখানে ডেটা দরকার।
* `value` স্থিতিশীল রাখতে `useMemo` ব্যবহার করো।
* Context ছোট ও শেয়ার করা state-এর জন্য উপযুক্ত (যেমন theme, auth, locale)।

---

### 🟣 B — Lifting State Up

যদি দুই sibling কম্পোনেন্টে একই state দরকার হয়, সেটি common parent-এ নিয়ে যাও এবং props হিসেবে পাঠাও।

---

### 🔵 C — Custom Hooks + Composition

একটি custom hook তৈরি করো (যেমন `useAuth`) যেখানে logic থাকবে, এবং সেটি কম্পোনেন্টে ব্যবহার করো। Hook-এর ভেতরে context ব্যবহার করা যায়।

---

### 🟠 D — State Management Libraries

যখন অ্যাপ জটিল বা global state বড় হয়, তখন ব্যবহার করো:

* **Redux / Redux Toolkit** → বড় প্রজেক্টে খুব কার্যকর।
* **Zustand / Jotai / Recoil** → ছোট ও আধুনিক অ্যাপের জন্য সহজ বিকল্প।

---

### 🟣 E — Render Props / Children-as-Function / Composition

অনেক props না পাঠিয়ে সরাসরি ফাংশন বা composition ব্যবহার করে প্রয়োজনীয় ডেটা অ্যাক্সেস করো।

---

## 6️⃣ Practical Tips & Best Practices

✅ shallow (১–২ লেভেল) props হলে সরাসরি props পাঠাও।
✅ Context কে আলাদা করে ভাগ করো — যেমন `AuthContext`, `ThemeContext`।
✅ `useMemo` দিয়ে provider value স্থিতিশীল রাখো:

```js
const value = useMemo(() => ({user, setUser}), [user]);
```

✅ `React.memo` ব্যবহার করো যাতে অপ্রয়োজনীয় re-render না হয়।
✅ Provider এর boundary ছোট রাখো — পুরো অ্যাপকে context এ মুড়িও না।
✅ Provider এর নাম স্পষ্ট রাখো (যেমন `AuthProvider`, `ThemeProvider`)।
✅ কোডে মন্তব্য বা README রাখো কোন provider কোথায় ব্যবহৃত তা বোঝানোর জন্য।
✅ Performance-critical অ্যাপে React DevTools Profiler দিয়ে re-render মাপো।

---

## 7️⃣ Debugging & Testing Prop Drilling

🔍 **Prop ব্যবহার কোথায় হচ্ছে খুঁজতে:** prop নাম দিয়ে সার্চ করো।
🧪 **Console.log** করে props flow চেক করো।
🧩 **Unit test** এ mock props ব্যবহার করো।
🪞 **React DevTools** দিয়ে props/context ইনস্পেক্ট করো।

---

## 8️⃣ Quick Decision Checklist

| পরিস্থিতি                       | কী করবো                  |
| ------------------------------- | ------------------------ |
| Prop মাত্র ১–২ লেভেল নিচে দরকার | সরাসরি props পাঠাও       |
| অনেক লেভেলে দরকার               | Context API ব্যবহার করো  |
| অনেক কম্পোনেন্টে দরকার          | Context বা state library |
| অ্যাপ বড় ও জটিল                 | Redux / Zustand          |
| অনেক props ফরওয়ার্ড হচ্ছে       | Refactor করে Context দাও |

---

## 9️⃣ ছোট Refactor Plan (Step-by-Step)

1️⃣ কোন props বারবার ফরোয়ার্ড হচ্ছে তা খুঁজে বের করো।
2️⃣ Context Provider তৈরি করে প্রাসঙ্গিক subtree তে wrap করো।
3️⃣ Consumer কম্পোনেন্টে `useContext` ব্যবহার করো।
4️⃣ React DevTools দিয়ে render behavior চেক করো।
5️⃣ জটিলতা বাড়লে Zustand / Redux Toolkit ব্যবহার বিবেচনা করো।

---

## 🎯 সারসংক্ষেপ

| বিষয়              | ব্যাখ্যা                                        |
| ----------------- | ----------------------------------------------- |
| **Prop Drilling** | ডেটা একাধিক স্তরে অপ্রয়োজনীয়ভাবে পাঠানো         |
| **সমস্যা**        | Boilerplate, maintain কঠিন, performance issue   |
| **সমাধান**        | Context API, Lifting state, custom hooks, Redux |
| **Best Practice** | ছোট scope এ props, বড় scope এ context/store     |

---
